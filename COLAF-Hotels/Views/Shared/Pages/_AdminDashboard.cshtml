
<div class="flex flex-col lg:flex-row gap-6">
    <!-- Left Column - 2/3 width on large screens -->
    <div class="lg:w-2/3 space-y-6">
        <!-- Sites Overview Card -->
        <div class="bg-white rounded-xl shadow-lg overflow-hidden border border-coffee-cream">
            <div class="p-6">
                <div class="flex justify-between items-center mb-4">
                    <h2 class="text-xl font-bold text-coffee-dark">Sites Overview</h2>
                    <select class="bg-coffee-cream text-coffee-dark rounded-lg px-3 py-1 text-sm font-medium">
                        <option>Last 7 days</option>
                        <option>Last 30 days</option>
                        <option>This month</option>
                    </select>
                </div>
                <div class="bg-gray-100 rounded-lg p-4 h-48 flex items-center justify-center mb-4">
                    <div class="w-full h-full bg-gradient-to-r from-coffee-light to-coffee-accent opacity-20 rounded-lg flex items-center justify-center">
                        <span class="text-coffee-dark opacity-100 font-medium">Daily Visits Graph</span>
                    </div>
                </div>
                <div class="grid grid-cols-3 gap-4 text-center">
                    <div class="bg-coffee-cream bg-opacity-20 p-4 rounded-lg">
                        <p class="text-sm text-coffee-medium">Total Visits</p>
                        <p class="text-xl font-bold text-coffee-dark mt-1">12,450</p>
                    </div>
                    <div class="bg-coffee-cream bg-opacity-20 p-4 rounded-lg">
                        <p class="text-sm text-coffee-medium">Daily Visits</p>
                        <p class="text-xl font-bold text-coffee-dark mt-1">360</p>
                    </div>
                    <div class="bg-coffee-cream bg-opacity-20 p-4 rounded-lg">
                        <p class="text-sm text-coffee-medium">Total Sales</p>
                        <p class="text-xl font-bold text-coffee-dark mt-1">$32,240</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Two Column Section -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Guest Overview Card -->
            <div class="bg-white rounded-xl shadow-lg p-6 border border-coffee-cream">
                <div class="flex items-center mb-4">
                    <i class="fas fa-users text-coffee-accent mr-3 text-xl"></i>
                    <h2 class="text-lg font-bold text-coffee-dark">Guest Overview</h2>
                </div>
                <div class="space-y-4 mb-6">
                    <div class="flex justify-between items-center">
                        <p class="text-coffee-medium">Total Guests</p>
                        <p class="font-bold text-coffee-dark">2,800</p>
                    </div>
                    <div class="flex justify-between items-center">
                        <p class="text-coffee-medium">Online Guests</p>
                        <div class="flex items-center">
                            <span class="h-2 w-2 bg-green-500 rounded-full mr-2"></span>
                            <p class="font-bold text-coffee-dark">74</p>
                        </div>
                    </div>
                </div>
                <button class="w-full bg-coffee-accent hover:bg-coffee-medium text-white font-medium py-2 px-4 rounded-lg transition duration-300 ease-in-out transform hover:-translate-y-1">
                    Manage Users
                </button>
            </div>

            <!-- Bookings Card -->
            <div class="bg-white rounded-xl shadow-lg p-6 border border-coffee-cream">
                <div class="flex items-center mb-4">
                    <i class="fas fa-book-open text-coffee-accent mr-3 text-xl"></i>
                    <h2 class="text-lg font-bold text-coffee-dark">Bookings</h2>
                </div>
                <div class="grid grid-cols-2 gap-4 mb-6">
                    <div>
                        <p class="text-coffee-medium text-sm">Total Bookings</p>
                        <p class="font-bold text-coffee-dark text-lg">180</p>
                    </div>
                    <div>
                        <p class="text-coffee-medium text-sm">Current Bookings</p>
                        <p class="font-bold text-coffee-dark text-lg">60</p>
                    </div>
                    <div>
                        <p class="text-coffee-medium text-sm">Total Check-ins</p>
                        <p class="font-bold text-coffee-dark text-lg">150</p>
                    </div>
                    <div>
                        <p class="text-coffee-medium text-sm">Current Check-ins</p>
                        <p class="font-bold text-coffee-dark text-lg">30</p>
                    </div>
                </div>
                <button class="w-full bg-coffee-accent hover:bg-coffee-medium text-white font-medium py-2 px-4 rounded-lg transition duration-300 ease-in-out transform hover:-translate-y-1">
                    View Bookings
                </button>
            </div>
        </div>

        <!-- Rooms Overview Cards -->
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
            <div class="bg-white rounded-xl shadow-lg p-6 border-l-4 border-coffee-accent border-t border-r border-b border-coffee-cream">
                <div class="flex flex-col items-center">
                    <i class="fas fa-door-closed text-coffee-accent text-2xl mb-3"></i>
                    <h3 class="text-3xl font-bold text-coffee-dark">120</h3>
                    <p class="text-coffee-medium mt-1">Occupied Rooms</p>
                </div>
            </div>
            <div class="bg-white rounded-xl shadow-lg p-6 border-l-4 border-yellow-500 border-t border-r border-b border-coffee-cream">
                <div class="flex flex-col items-center">
                    <i class="fas fa-tools text-yellow-500 text-2xl mb-3"></i>
                    <h3 class="text-3xl font-bold text-coffee-dark">15</h3>
                    <p class="text-coffee-medium mt-1">Rooms in Maintenance</p>
                </div>
            </div>
            <div class="bg-white rounded-xl shadow-lg p-6 border-l-4 border-green-500 border-t border-r border-b border-coffee-cream">
                <div class="flex flex-col items-center">
                    <i class="fas fa-door-open text-green-500 text-2xl mb-3"></i>
                    <h3 class="text-3xl font-bold text-coffee-dark">85</h3>
                    <p class="text-coffee-medium mt-1">Vacant Rooms</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Right Column - Sidebar -->
    <div class="lg:w-1/3 space-y-6">
        <!-- Time & Date Card -->
        <div class="bg-white rounded-xl shadow-lg p-6 border border-coffee-cream">
            <h2 class="text-xl font-bold text-coffee-dark mb-4">Dashboard</h2>
            <div class="bg-coffee-cream bg-opacity-30 rounded-lg p-6 text-center">
                <p id="dateDisplay" class="text-coffee-medium">March 14, 2025</p>
                <p id="clockDisplay" class="text-3xl font-bold text-coffee-dark mt-2">12:30 PM</p>
            </div>
        </div>

        <!-- Calendar Card -->
        <div class="bg-white h-[24rem] p-2 rounded-xl shadow-lg border border-coffee-cream">
                <div id="calendar" class="w-full">
                </div>

        </div>

        <!-- Quick Actions Card -->
        <div class="bg-white rounded-xl shadow-lg p-6 border border-coffee-cream">
            <h2 class="text-lg font-bold text-coffee-dark mb-4">Quick Actions</h2>
            <div class="space-y-3">
                <button class="w-full bg-coffee-medium hover:bg-coffee-dark text-white font-medium py-2 px-4 rounded-lg transition duration-300 flex items-center justify-center">
                    <i class="fas fa-plus-circle mr-2"></i> New Booking
                </button>
                <button class="w-full bg-coffee-light hover:bg-coffee-medium text-white font-medium py-2 px-4 rounded-lg transition duration-300 flex items-center justify-center">
                    <i class="fas fa-check-circle mr-2"></i> Quick Check-in
                </button>
                <button class="w-full bg-coffee-cream hover:bg-coffee-accent text-coffee-dark font-medium py-2 px-4 rounded-lg transition duration-300 flex items-center justify-center">
                    <i class="fas fa-clipboard-list mr-2"></i> Room Status
                </button>
            </div>
        </div>
    </div>
</div>
<script>
    document.addEventListener('DOMContentLoaded', function () {
        var calendarEl = document.getElementById('calendar');
        var calendar = new FullCalendar.Calendar(calendarEl, {
            initialView: 'dayGridMonth',
            headerToolbar: {
                left: 'prev,next', // Removed 'today' button
                center: 'title',
                right: 'dayGridMonth'
            },
            height: 350,
            aspectRatio: 2,
            events: '/Event/GetEvents',

            // Add hover effect for events
            eventMouseEnter: function(info) {
                let tooltip = document.createElement("div");
                tooltip.className = "fc-tooltip";
                tooltip.innerText = info.event.title;
                document.body.appendChild(tooltip);

                tooltip.style.position = "absolute";
                tooltip.style.background = "rgba(0, 0, 0, 0.8)";
                tooltip.style.color = "white";
                tooltip.style.padding = "5px 10px";
                tooltip.style.borderRadius = "5px";
                tooltip.style.fontSize = "12px";
                tooltip.style.pointerEvents = "none";
                tooltip.style.zIndex = "1000";
                tooltip.style.top = (info.jsEvent.clientY + 10) + "px";
                tooltip.style.left = (info.jsEvent.clientX + 10) + "px";

                info.el.setAttribute("data-tooltip-id", tooltip);
            },
            eventMouseLeave: function(info) {
                let tooltip = document.querySelector(".fc-tooltip");
                if (tooltip) tooltip.remove();
            }
        });

        calendar.render();

        // Function to resize calendar text & style elements
        function resizeCalendarText() {
            document.querySelectorAll('#calendar *').forEach(element => {
                element.style.fontSize = '14px'; // Change all text to 14px
            });

            // Specific text size adjustments
            document.querySelectorAll('.fc-toolbar-title').forEach(el => el.style.fontSize = '18px'); // Month title
            document.querySelectorAll('.fc-daygrid-day-number').forEach(el => el.style.fontSize = '12px'); // Day numbers
            document.querySelectorAll('.fc-event-title').forEach(el => el.style.fontSize = '10px'); // Event text
            document.querySelectorAll('.fc-col-header-cell').forEach(el => el.style.fontSize = '14px'); // Weekdays (Sun, Mon, etc.)

            // Style buttons
            document.querySelectorAll('.fc-button').forEach(button => {
                button.style.fontSize = '12px'; // Smaller button text
                button.style.padding = '5px 10px'; // Adjust padding
                button.style.height = '30px'; // Adjust button height
                button.style.backgroundColor = '#4E3629'; // Dark brown background
                button.style.border = 'none'; // Remove border
                button.style.color = 'white'; // White text
                button.style.borderRadius = '5px'; // Rounded edges
                button.style.fontWeight = 'bold'; // Bold text
                button.onmouseover = () => (button.style.backgroundColor = '#8B5E3C'); // Hover effect
                button.onmouseout = () => (button.style.backgroundColor = '#4E3629');
            });

            // Highlight the current day
            document.querySelectorAll('.fc-day-today').forEach(day => {
                day.style.backgroundColor = '#FFD700'; // Gold background for current day
                day.style.color = '#4E3629'; // Dark text for contrast
                day.style.borderRadius = '5px'; // Rounded edges
                day.style.fontWeight = 'bold';
            });
        }

        // Run initially after rendering
        setTimeout(resizeCalendarText, 500);

        // Use MutationObserver to detect changes in calendar structure & reapply styles
        const observer = new MutationObserver(() => {
            resizeCalendarText();
        });

        observer.observe(calendarEl, { childList: true, subtree: true });
    });

        function updateDateTime() {
        const now = new Date();
        const options = { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' };
        document.getElementById('dateDisplay').textContent = now.toLocaleDateString('en-US', options);
        document.getElementById('clockDisplay').textContent = now.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit', second: '2-digit' });
    }

    setInterval(updateDateTime, 1000);
    updateDateTime(); // Initialize immediately
</script>




